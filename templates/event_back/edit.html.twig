{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                <h2>Edit Event</h2>
            </div>
            <div class="card-body">
                <p class="mb-3">You can update the details of your event below.</p>

                {{ include('event_back/_form.html.twig', {'button_label': 'Update', 'class': 'mb-3'}) }}

                <a href="{{ path('app_event_back_index') }}" class="btn btn-secondary mb-3">Back to list</a>

                {{ include('event_back/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
